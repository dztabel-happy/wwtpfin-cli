# 污水处理财务领域契约

本文件定义产品内核的唯一语义。案例文件、Excel 公式、Word 正文和报告版式均不得
改变这里的概念。

## 1. 产品边界

产品主链路是：

`结构化项目参数与证据 → 领域计算 → 完整咨询内容包 → 可选呈现适配器`

真实项目只作为产品测试中的 benchmark，不成为领域参数、默认值或通用算法分支。

## 2. 四类信息

| 类型 | 含义 | 可否参与计算 |
|---|---|---|
| 项目事实 | 项目名称、规模、合同日期、资产状况等 | 仅结构化字段可参与 |
| 建模选择 | 水量、价格、成本、融资、税务的计算方法 | 可以，必须显式记录 |
| 政策规则 | 生效日期、适用范围、证据来源 | 只约束适用性，不替代项目事实 |
| 案例兼容 | 源模型错误时点、错位公式、旧报告措辞 | 只能在 benchmark/legacy 输出使用 |

## 3. 领域不变量

### 时间

- 同一输入在不同运行日期必须得到相同结果。
- `evaluation_date` 是日期判断的唯一基准；领域层不得调用系统当前日期。
- 证据日期不得晚于 `evaluation_date`。
- 项目时间轴由前期时点和运营期组成：有建设期时按建设年度列示，无建设期时使用独立
  `t=0`；收入、成本和税费从投运后开始。

### 水量

- `demand_m3d`：服务需求预测。
- `treated_m3d`：设施实际处理量。
- `billed_m3d`：合同计费量。
- `opex_driver_m3d`：成本计量基数。
- 四者必须分别生成和输出；改变某一口径的开关不得隐式改变其它口径。
- 副产品产量只能由 `treated_m3d` 或显式产量产生。

### 收入

- 每条收入流分别保存数量、含税金额、不含税金额、销项税和资金来源。
- 服务费、容量费、绩效扣减、政策性运营补贴和副产品收入不得只保留合计。
- 报表标签必须绑定同名收入流；总营业收入不得标成污水处理服务费。

### 税务

- 税收优惠按收入流判断适用范围，不能自动扩散到其它收入。
- 服务费与副产品销项、进项和留抵分别核算；经营期共享进项按销售额或收入占比分摊，
  长期资产混合用途进项不得用经营期收入比例替代法定调整口径。
- 一般计税与简易计税分别建模；简易计税收入流不得抵扣对应进项税。
- 税收适用性证据与税额计算分离；计算结果不能自行证明优惠资格。
- 三免三减半必须核验项目属于新建、扩建或提标改造，并提供验收、连续达标和起算证据；纯存量转让不能从案例口径继承优惠资格。
- 资源综合利用收入减计只调整所得税税基，不改变会计收入；专用设备抵免在所得减免后
  抵减应纳税额，并按规定期限结转。

### 投资、资产与融资

- 分年基础投资及建设期利息必须与资本金、债务提用和实际用于投资的建设补助闭合。补助用途和比例基数由 `capital.funding` 明确，不能在减少筹资后重复计入股东收益；见[资金结构契约](docs/FUNDING_STRUCTURE.md)。给出分年提款计划时以该计划为准，声明的资本金/债务比例只作对照、不硬闭合。
- 运营资本支出须区分维持运营与扩建并保留工程依据；DSCR 只按已声明评价口径扣减，全部支出始终进入现金流。偿债比率合格不能替代资金闭合。
- 初始取得投资、建设投资、资本金投入和长期债务提款位于同一真实前期时点；不得进入
  运营第 1 年。运营期投资活动只含重置投资、运营期铺底流动资金投入/回收、期末资产事件，运营期筹资活动只含运营期
  融资（含铺底 funding_source=equity 时的同额资本金流入）、还本付息及分红；完整财务计划使用独立全周期时点序列。
- 建设投资的费用构成与资产化去向是两套独立分类，两者分别与投资合计闭合。
- 资产按固定资产、无形资产、土地和流动资金分类；建设期利息只分配给可资本化非流动资产。
- 建设期利息选择费用化时仍进入总投资与资金筹措，不进入资产原值；是否形成可结转税务亏损必须显式声明，不得由产品猜测。
- `calculation_basis.project_cashflow_initial_investment` 只控制全投资评价现金流是否包含资本化建设期利息；项目总投资、资产原值、资金筹措、资本金现金流和财务计划始终按实际资本化与收付边界列示。
- 贷款期包含宽限期；支持等额本息、等额本金和按期付息到期还本，逐笔贷款分别配置。
  不规则还本的官方姿势是多笔 bullet 分档叠加（例：本金 60 分三笔
  amount_wan=10/20/30、term_years=2/3/4、repayment_method=bullet、同利率），
  不提供原生还本序列字段。
- 运营期长期贷款只允许绑定同年发生的重置或扩建资产；提款金额不得超过关联资产投资额，无明确用途的运营期融资不得进入模型。
- 模式名称不切换算法；模式必须与存量对价、建设期和资产构成一致。
- 期末资产移交或处置是独立项目事件；建设和改造类模式必须显式声明无偿移交、
  净账面值处置或税后处置价值，BOO 必须显式声明继续持有资产的期末价值口径。
- 无偿移交不产生现金流，但必须确认账面资产退出及其处置损益；不得把剩余账面价值
  留在项目终止后的资产负债表中。

### 成本

- 项目支出、服务费及补助、指定主体净支出分别核算，不能相加或相互改名。新增指标与逐期底账遵循 [成本口径契约](docs/COST_PERSPECTIVES.md)。
- 指定主体收支范围不完整时，完整净支出指标为空；允许保留已列底账和条件估算。
- 成本方案比较须保持服务与期间一致，现值比较须保持折现率一致；单价代理还须保持支付函数和补助序列一致。

- 每个成本科目独立声明是否随水量变化及年度增长率。
- 大修等非均匀支出使用显式逐年金额；不得用年均值冒充真实现金流时点。
- 项目级/科目级阶梯递增（自运营第 S 年起每 I 年乘 (1+r)）与科目年度复合增长率、
  显式逐年金额互斥；大修等非均匀科目用逐年金额并豁免项目级阶梯。
- 派生科目按具名非派生科目含税发生额的比例生成，v1 禁止嵌套派生；
  阶梯先作用于基项、派生自动跟随。价税分离、vat_scope、调价归组属于派生科目自身。

### 政策与决策

- 财务计算不能推出政策合规、模式适用、公众支付意愿或项目必要性。
- 债务融资利率与社会资本的市场参考收益率是两个概念；后者未提供时，吸引力只能判为无法判断。
- 政策规则只能产生“满足/不满足/证据不足/不适用”的结构化发现。
- 结算渠道不等于资金来源；政府代收转付不等于财政补贴。
- 新机制项目允许依法依规、具有普适依据的运营补贴，不能把所有财政资金一律拒绝。
- 财务基准收益率由输入按情景提供；每个情景必须记录口径、来源、核查日期、提供方和
  证据状态。内核不预置项目常量或未经核验的行业门槛。
- 只有证据状态为 `verified` 的基准可支持肯定或否定的盈利能力裁决；`reference` 与
  `unverified` 只用于参考或敏感性比较。

### 输出与 benchmark

- `run` 是唯一计算契约；`build` 是唯一完整成果契约，且只消费 `run` 的 JSON 数据。
- 完整成果必须同时包含文档语义、Markdown 投影、计算结果、证据总账、质量结果、全部表格数据和图表语义；不得依赖 LLM 补写业务内容。
- `document.json` 是正文语义的权威来源；`report.md` 必须是其确定性投影，不得增加或改变结论。
- 每个判断必须引用证据；每个表格、图表引用必须在索引中闭合；同一输入必须生成逐字节一致的成果包。
- 非财务裁决证据使用 `wwtp-fin/project-evidence/1` 的语义键，不得使用案例模板占位符命名。
- 产品输入必须显式声明期限、规模、模式、融资、税制、收入机制、成本边界和基准情景；
  内部构造默认值不得成为 CLI 的隐含项目假设。
- 每个成本、资产、贷款和副产品项目的物理口径、增长、税率、期限及归属必须逐项声明；
  只有未启用模块的参数可以省略。
- benchmark 通过只证明复现案例，不证明通用算法正确。
- 通用性质测试、组合测试和会计恒等式校验必须独立于案例存在。
- 多方案比较只运行使用者明确给出的有限候选；机械排序不构成推荐，最终参数必须由用户确认后进入新的不可变运行。

## 4. 组合原则

TOT、BOT、ROT、BOOT、DBFOT、O&M、改扩建和混合项目由同一组水量、收入、
成本、投资、资产、融资、税务和决策规则组合，不为模式缩写复制算法。
