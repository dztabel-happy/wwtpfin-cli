# Minimal complete showcase

`project.yaml` and `evidence.json` are the smallest complete public input pair.

```bash
wwtp-fin build -p project.yaml --evidence evidence.json -o deliverable
wwtp-fin verify-deliverable deliverable
wwtp-fin check -p project.yaml
```

The example demonstrates the input and evidence contract only; it is not a project template or a consulting conclusion.
