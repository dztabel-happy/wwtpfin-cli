# 成本、合同支付与指定主体净支出

“降低建设期和运营期总成本”需要先分清项目花多少钱、合同付多少钱、指定主体最终承担多少钱。新指标引用同一次计算的逐期底账，不改变原财务报表、税额、现金流或偿债结果，可用于 `compare` 的目标或硬约束。

| 指标前缀 | 纳入范围 | 主要限制 |
|---|---|---|
| `investment_opex_outlay` | 不含建设期利息的初始投资 + 运营期资本支出 + 含税经营支出 | 含存量取得价款，非社会资源成本；投资沿用输入增值税口径 |
| `project_cash_cost_net` | 初始投资及建设期利息 + 运营资本支出、经营、融资费用、实际现金税费、流动资金投入 − 流动资金回收、退税、期末价值 | 不扣营业收入、补助；不重复计贷款本金、资本金或摊销。融资不可唯一重建时为空 |
| `service_grant_payment` | 含税服务费 + 实际到位资本补助 | 不含副产品收入与期末支付；不是财政净负担 |
| `payer_net_outflow` | 按明确比例归属服务费、补助、期末支付 + 另列支出 − 另列收入 | 仅对应指定主体、声明范围；未建模或范围不完整时为空 |

每个前缀有 `_wan`（名义合计，万元）和 `_npv_wan`（现值，万元）两个指标。现值为 `sum(C[t] / (1 + discount_rate)^t)`；第 0 期不折现。建设期、运营期及期末沿用模型整年期次，不得称为精确半年或日历现金流。

初始与重置投资沿用各自已声明的增值税口径。采用 `net_of_deductible_vat` 的投资不是含税工程发票总额；工程发票或财政预算审计需要对应现金与税务底稿。各视角不能相加；合同支付已为项目支出提供资金，重复相加会重复计算。既有 `total_lifecycle_cost_wan` / `total_lifecycle_net_cost_wan` 保留原定义兼容旧成果。

## 指定主体收支输入

`payer_cashflow` 是可选输入。省略或 `{}` 表示未建模，净支出指标为 `null`。完整对象须同时提供：

- `entity`、`scope`、`basis`、非空 `evidence_refs`：主体、收支范围及依据。
- `coverage: partial | complete`：范围是否完整的输入声明，CLI 不代替审核人认证。
- `service_payment_share`、`capital_grant_payment_share`、`terminal_payment_share`：0 至 1 的支付归属比例，不根据结算渠道自动推断。
- `other_cashflows`：另列收支，每项包含唯一 `id`、`label`、`direction: inflow | outflow`、`amounts_wan`、`basis`、`evidence_refs`。金额为有限非负数，长度为 `1 + construction_years + concession_years`，包括第 0 期。服务费、补助、期末价款已自动归属，不得重复计入另列收支。

例如政府代收用户污水处理费再支付运营方，应分别列服务费支付与代收收入。收款范围、时间和金额缺失时保留 `partial`，输出已列收支底账；不能升级为完整净支出目标。税费不自动抵扣为同一主体收入，借款也不自动成为降本收入。外部收支及适用范围仍需业务人员核实。

有依据的估算可开展明确的条件情景，范围完整不代表金额已核定。估算边界与落实条件进入证据和决策合同，不用等全部资料核定才进行探索。

## 比较与成果

有决策合同的上述成本目标自动核对建设及运营期间、实际处理水量；现值目标还核对折现率，指定主体目标还核对主体及范围。改变这些要素的候选保留计算结果，标为 `not_comparable`，不参与可选排序。出水标准、可靠性及未被数值表达的服务边界仍须在固定参数与实施条件中约束。

`tariff_blended_gross` 只在服务费计取函数与补助序列不变时可作支付代理。补助、计费量、调价、阶梯机制变化时应改用完整支付指标，不能凭起始单价较低声称总成本较低。`numerically_feasible`、`comparable`、实施条件与用户选择分别记录；`feasible` 同时要求数值及可比口径通过。无合同的探索结果不生成正式选定成果。

通用反例：单价从 4.00 降至 3.99 元，同时增加 1,000 万元资本补助；服务费减少 254.5875 万元，合计支付增加 745.4125 万元。产品阻止用单价对该变化直接排序，按完整支付可正确比较。此算例是产品验收，不是政策建议或项目报价。

`result.json.costs` 保存逐期组成、口径说明与主体收支，`scalars` 保存 8 个指标。成果包增加正文用途的 `cost_perspectives` 表及工作底稿用途的 `cost_cashflows`、条件生成的 `payer_cashflows`；方案比较同时列各候选的成本口径。

## 制度背景与能力边界

2024 年《基础设施和公用事业特许经营管理办法》第六条区分直接收费与政府等代收，第十四条涉及全生命周期成本比较，第二十二条对代收转付和政府投资支持规定相应条件。这支持分开核算收入来源、支付路径和成本，不意味着任意财政支持方案均被允许。[国家发展改革委公布原文](https://zfxxgk.ndrc.gov.cn/web/iteminfo.jsp?id=20369)

本模块计算已声明机制，不判断政策资格、不建立全口径政府预算、不自动设计融资、不证明全局最优。任意多变量搜索、精确半年期次或运营期股东增资仍须按实际支持边界处理。
